# Python dependencies for the Paper A reproducibility pipeline.
# Tested with Python 3.11-3.13 (macOS, Homebrew python3).
# These four are everything the code imports outside the standard library.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1        # .xlsx table workbooks
